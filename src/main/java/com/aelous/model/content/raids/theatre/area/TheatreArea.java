package com.aelous.model.content.raids.theatre.area;

import com.aelous.model.content.instance.InstanceConfiguration;
import com.aelous.model.content.instance.InstancedArea;
import com.aelous.model.map.position.Area;

public class TheatreArea extends InstancedArea {
    public TheatreArea(InstanceConfiguration configuration, Area... areas) {
        super(configuration, areas);
    }
}
